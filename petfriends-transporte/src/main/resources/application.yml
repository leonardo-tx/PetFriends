server:
  port: 8083

spring:
  application:
    name: petfriends-transporte

  data:
    mongodb:
      uri: ${MONGODB_URI}
      database: ${MONGODB_DATABASE}
      auto-index-creation: true

  cloud:
    stream:
      function:
        autodetect: false

axon:
  axonserver:
    enabled: false

management:
  endpoints:
    web:
      exposure:
        include: health