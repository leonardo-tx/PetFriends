apiVersion: v1
kind: Service
metadata:
  namespace: {{.Release.Namespace}}
  name: "{{.Values.transporte_db.name}}-svc"
spec:
  selector:
    app: {{ .Values.transporte_db.name }}
  type: {{.Values.transporte_db.service.type}}
  ports:
    - protocol: TCP
      {{- if (eq .Values.transporte_db.service.type "NodePort")}}
      nodePort: {{.Values.transporte_db.service.nodePort}}
      {{- end}}
      targetPort: {{.Values.transporte_db.service.targetPort}}
      port: {{.Values.transporte_db.service.port}}
