apiVersion: v1
kind: Service
metadata:
  namespace: {{.Release.Namespace}}
  name: "{{.Values.petfriends_transporte.name}}-svc"
spec:
  selector:
    app: {{ .Values.petfriends_transporte.name }}
  type: {{.Values.petfriends_transporte.service.type}}
  ports:
    - protocol: TCP
      {{- if (eq .Values.petfriends_transporte.service.type "NodePort")}}
      nodePort: {{.Values.petfriends_transporte.service.nodePort}}
      {{- end}}
      targetPort: {{.Values.petfriends_transporte.service.targetPort}}
      port: {{.Values.petfriends_transporte.service.port}}
