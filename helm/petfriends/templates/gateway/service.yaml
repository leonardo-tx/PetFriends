apiVersion: v1
kind: Service
metadata:
  namespace: {{.Release.Namespace}}
  name: "{{.Values.petfriends_gateway.name}}-svc"
spec:
  selector:
    app: {{ .Values.petfriends_gateway.name }}
  type: {{.Values.petfriends_gateway.service.type}}
  ports:
    - protocol: TCP
      {{- if (eq .Values.petfriends_gateway.service.type "NodePort")}}
      nodePort: {{.Values.petfriends_gateway.service.nodePort}}
      {{- end}}
      targetPort: {{.Values.petfriends_gateway.service.targetPort}}
      port: {{.Values.petfriends_gateway.service.port}}
