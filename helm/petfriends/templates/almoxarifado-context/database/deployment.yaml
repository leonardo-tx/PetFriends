apiVersion: apps/v1
kind: Deployment
metadata:
  name: "{{ .Values.almoxarifado_db.name }}-deploy"
  namespace: {{ .Release.Namespace }}
spec:
  replicas: {{ .Values.almoxarifado_db.replicas }}
  selector:
    matchLabels:
      app: {{ .Values.almoxarifado_db.name }}
  template:
    metadata:
      labels:
        app: {{ .Values.almoxarifado_db.name }}
    spec:
      containers:
        - name: {{ .Values.almoxarifado_db.name }}
          image: "{{ .Values.almoxarifado_db.image }}:{{ .Values.almoxarifado_db.tag }}"
          imagePullPolicy: {{ .Values.almoxarifado_db.imagePullPolicy }}
      restartPolicy: Always
