apiVersion: v1
kind: Service
metadata:
  namespace: {{.Release.Namespace}}
  name: "{{.Values.petfriends_pedido.name}}-svc"
spec:
  selector:
    app: {{ .Values.petfriends_pedido.name }}
  type: {{.Values.petfriends_pedido.service.type}}
  ports:
    - protocol: TCP
      {{- if (eq .Values.petfriends_pedido.service.type "NodePort")}}
      nodePort: {{.Values.petfriends_pedido.service.nodePort}}
      {{- end}}
      targetPort: {{.Values.petfriends_pedido.service.targetPort}}
      port: {{.Values.petfriends_pedido.service.port}}
